node_modules
dist
*.local
.DS_Store
shots

# Промежуточные сборки не храним, а два готовых файла — храним:
# заказчику нужно скачивать их прямо с GitHub, без npm.
*.html
!index.html
!Аква пати — CRM.html
!Аква пати — CRM (пустая касса).html
